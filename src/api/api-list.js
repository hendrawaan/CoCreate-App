const API_URL = "http://54.158.203.226:80";

// Login page.
export const LOGIN = `${API_URL}/api/v1/login`;

// Mendaftarkan user baru.
export const ADD_USER = `${API_URL}/api/v1/user/add`;

// Menampilkan semua daftar user.
export const LIST_USERS = `${API_URL}/api/v1/users`;
